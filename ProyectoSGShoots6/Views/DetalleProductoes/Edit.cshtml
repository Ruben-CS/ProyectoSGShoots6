@model ProyectoSGShoots6.Models.DetalleProducto
@{
    ViewData["Title"] = "Edit";
}

<h1>Editar</h1>

<h4>DetalleProducto</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="PaquetesId" />
            <input type="hidden" asp-for="ProductosId" />
            <div class="form-group">
                <label asp-for="UnidadMedidaId" class="control-label"></label>
                <select asp-for="UnidadMedidaId" class="form-control" asp-items="ViewBag.UnidadMedidaId"></select>
                <span asp-validation-for="UnidadMedidaId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
